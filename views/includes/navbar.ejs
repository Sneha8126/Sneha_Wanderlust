<nav class="navbar navbar-expand-lg bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"><i class="fa-solid fa-compass-drafting"></i></a>
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item"><a class="nav-link fw-semibold text-dark" href="/listings">Explore</a></li>
      </ul>

      <div class="navbar-nav ms-auto align-items-lg-center">
        
        <form class="d-flex my-2" action="/listings" method="GET">
          <input class="form-control rounded-pill search-inp" type="search" name="search" placeholder="Search..." value="<%= typeof search !== 'undefined' && search ? search : '' %>">
          <button class="btn search-btn" type="submit"><i class="fa-solid fa-magnifying-glass"></i></button>
        </form>

        <% if(currUser && currUser.role === 'host') { %>
          <a class="nav-link fw-semibold text-dark" href="/listings/new">Airbnb your home</a>
        <% } %>
        
        <% if(!currUser){ %>
          <a class="nav-link fw-semibold text-dark" href="/signup">Sign up</a>
          <a class="nav-link fw-semibold text-dark" href="/login">Log in</a>
        <% } else { %>
          <a class="nav-link fw-semibold text-dark" href="/wishlist"><i class="fa-regular fa-heart"></i> Wishlist</a>
          <a class="nav-link fw-semibold text-dark" href="/profile"><i class="fa-regular fa-user-circle"></i> Profile</a>
          <a class="nav-link fw-semibold text-dark" href="/logout">Log out</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>

<style>
  .search-btn { background-color: #fe424d; color: #fff; border-radius: 25px; padding: 0 1rem; margin-left: -40px; border: none; }
  .search-inp { border-radius: 25px; padding: 0.5rem 3rem 0.5rem 1rem; }
  
  @media (max-width: 992px) {
    .navbar-collapse { background: #f8f9fa; padding: 10px; border-radius: 10px; margin-top: 10px; }
    .nav-link { padding: 8px 0 !important; }
  }
</style>